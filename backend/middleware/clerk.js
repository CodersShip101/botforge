// DEPRECATED: Replaced by middleware/auth.js (JWT-based auth).
// This file is no longer imported. Kept for reference during migration.
// The new auth flow uses jsonwebtoken tokens via the /api/auth endpoints.
