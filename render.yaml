services:
  - type: web
    name: vantis-ai
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CLERK_PUBLISHABLE_KEY
        sync: false
      - key: CLERK_SECRET_KEY
        sync: false
      - key: NODE_ENV
        value: production
